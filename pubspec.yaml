name: flutter_nice_image_compress
version: 1.0.0
description: A powerful Flutter plugin for advanced image compression with intelligent algorithms that balance size and quality.
homepage: https://github.com/ke112/flutter_nice_image_compress
repository: https://github.com/ke112/flutter_nice_image_compress
issue_tracker: https://github.com/ke112/flutter_nice_image_compress/issues

environment:
  sdk: ^3.0.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  image: ^4.5.4
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
